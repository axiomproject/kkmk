[build]
  command = "yarn cache clean && yarn install --force && yarn build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18.20.0"
  YARN_VERSION = "1.22.19"
  NODE_OPTIONS = "--max_old_space_size=4096"

